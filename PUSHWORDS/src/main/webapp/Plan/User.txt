User
    Differentiate by user behavior      actionName
        Login                           actionName = "login"
        Personal center                 actionName = "userCenter"
        Load avatar                     actionName = "userHead"
        Verify nickname uniqueness      actionName = "checkNick"
        Modify user information         actionName = "updateUser"
        Exit                            actionName = "logout"


In UserServlet, service methods:

    1/ Get the action of user:
        String actionName = request.getParameter("actionName")
    2/ Judge the action:
        if("Login".equals(actionName)){
            // Login
        } else if ("userCenter".equals(actionName)){
            // Personal center
        }...


I. Login of User

       ================== Frontend =================

       1. add the loginForm
            action="user" method="post" id="loginForm"
       2. Set attribute values of loginForm ,modify the id attribute value (change the first letter to lowercase)
            Set the name attribute value of the text box and password box
            Set the check box, name attribute value and value attribute value (if the check box is selected, the value attribute value cannot be obtained)
            Set a hidden domain and pass user behavior (name = “actionName” value = “login”)
       3. checkLogin()


       =================== Backend =================


